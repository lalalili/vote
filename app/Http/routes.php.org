<?php

/*
|--------------------------------------------------------------------------
| Application Routes
|--------------------------------------------------------------------------
|
| Here is where you can register all of the routes for an application.
| It's a breeze. Simply tell Laravel the URIs it should respond to
| and give it the controller to call when that URI is requested.
|
*/

/*
|--------------------------------------------------------------------------
| API routes
|--------------------------------------------------------------------------
*/
Route::group(['prefix' => 'api', 'namespace' => 'App\Http\Controllers\API'], function () {
    Route::group(['prefix' => 'v1'], function () {
        include_once Config::get('generator.path_api_routes');
    });
});


Route::get('/', function () {
    return view('home');
});

Route::controller('Photo', 'PhotoController');

//Route::group(['prefix' => 'admin', 'middleware' => 'auth'], function() {
Route::group(['prefix' => 'admin'], function () {


    Route::controller('Photo', 'PhotoController');
//    Route::controller('vote','VoteController');
//    Route::controller('album','AlbumController');

//    Route::get('/', function(){
//        return view('admin.index');
//    });
//
//    Route::resource('photos', 'PhotoController');
//    Route::get('photos/{id}/delete', [
//        'as' => 'photos.delete',
//        'uses' => 'PhotoController@destroy',
//    ]);
//
//    Route::get('albums/upload', 'AlbumController@upload');
//    Route::post('albums/batch', 'AlbumController@batch');
//    Route::resource('albums', 'AlbumController');
//    Route::get('albums/{id}/delete', [
//        'as' => 'admin.albums.delete',
//        'uses' => 'AlbumController@destroy',
//    ]);
//
//    Route::resource('votes', 'VoteController');
//    Route::get('votes/{id}/delete', [
//        'as' => 'votes.delete',
//        'uses' => 'VoteController@destroy',
//    ]);
});